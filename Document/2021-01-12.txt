=> Today i have whatch and learn form videos.

=> Today i learn Parser
	- it's check code line by line and check the syntex error

=> Also i have create form and complete task.

=> Execution Context Object:
	- in this phase creation phase, Execution Phase

=> Hoisting:
	- In hoisting if we declere first then we initialize then it's also run in js. Ex: if we write first x=5 then we declere var x; then it is also run not generate error.

=> This keyword:
	- we write this keyword direct then it is by default point globle object. if create a object then use this keyword then this point to object.

=> Then i have also learn scss, css(Inline, Embeded, External), noscript, External JS, etc...
